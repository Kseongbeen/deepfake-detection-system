# EfficientNet-B4 백본 및 Triplet Loss 특징 공간 임베딩과 SVM을 활용한 딥페이크 변조 영상 탐지 성능 최적화 연구

**휴먼지능정보공학전공 인공지능개론 협업 프로젝트**  
* **지도 교수**: 휴먼지능정보공학과 윤희남 교수  
* **팀장**: 202210822 김성빈  
* **팀원**: 202210868 이현준, 202410800 정기준, 202421318 박동현, 202510695 김도윤, 202510698 김성운  
* **기간**: 2026. 03. 02. ~ 2026. 06. 15.  

---

## 목차
1. **서론**
   1.1. 연구 배경 및 목적
   1.2. 프로젝트 목표
   1.3. 멘토-멘티 협업
2. **이론적 배경**
   2.1. 합성곱 신경망(CNN) 및 EfficientNet-B4
   2.2. Triplet Loss (삼중항 손실 함수) 기반 임베딩 학습
   2.3. 서포트 벡터 머신 (SVM) 분류기
   2.4. 성능 평가 지표
3. **데이터 수집 및 전처리**
   3.1. 데이터 수집 개요
   3.2. 얼굴 영역 크롭(Face Crop) 및 데이터셋 구성
4. **실험 설계 및 구현 결과**
   4.1. 실험 설정 및 학습 환경
   4.2. EfficientNet-B4 + Triplet Loss + SVM 결합 모델 구현
   4.3. 성능 비교 평가
5. **결론 및 제언**
   5.1. 결론
   5.2. 한계점 및 제언

---

## 1. 서론

### 1) 연구 배경 및 목적

#### (1) 연구 배경
 딥 생성 모델(Generative Models)과 컴퓨터 비전 기술의 비약적인 발전으로 정교해진 딥페이크(Deepfake) 변조 영상은 가짜 뉴스 유포, 명예훼손, 금융 사기(보이스피싱 등), 여론 조작 등 심각한 사회적·윤리적 문제점을 야기하고 있다. 초기 딥페이크 영상은 눈 깜빡임 부자연스러움, 경계선 노이즈 등 시각적 왜곡이 뚜렷했으나, 최근 고도화된 생성 기법(예: GAN, Diffusion Model)으로 인해 사람의 육안이나 일반적인 CNN 분류기 모델로는 진짜와 가짜를 완벽하게 구별하는 것이 거의 불가능해졌다. 

 기존의 일반적인 딥페이크 변조 영상 탐지 기법들은 영상의 픽셀 수준에서의 미세한 텍스처 불일치나 프레임별 공간 정보를 단일 이진 분류기로 판별하는 방식에 의존했다. 그러나 이러한 방식은 학습 데이터셋에 과적합(Overfitting)되기 쉽고, 학습되지 않은 새로운 도메인(Cross-Domain)이나 다양한 화질 저하 환경에서 탐지 신뢰도가 급격히 떨어진다. 

 따라서 본 연구에서는 이 문제를 해결하기 위해, 인공지능 기반의 고성능 안면 특징 추출 모델인 EfficientNet-B4와 클래스 간 거리 제약을 직접 최적화하는 Triplet Loss(삼중항 손실) 함수 기반의 메트릭 러닝(Metric Learning) 기법을 융합한다. 이를 통해 변조 흔적이 극히 미세한 안면 영역에서 강건한 특징 공간 임베딩을 학습하고, 최종 분류 성능을 결정짓는 분류기로서 일반화 성능이 뛰어난 SVM(Support Vector Machine)을 결합하여 오탐지율을 최소화하는 딥페이크 탐지 기법을 설계하고자 한다.

#### (2) 연구 목적
 본 프로젝트는 다음과 같은 세 가지 주요 목표를 가지고 진행되었다.
1. **정량적 탐지 모델 설계 및 고도화**: 대규모 한국인 얼굴 및 딥페이크 영상 데이터셋(AIHub 및 데이콘 등)을 활용해 얼굴 이미지를 고밀도로 추출하는 MTCNN 파이프라인을 정립하고, EfficientNet-B4 아키텍처를 기반으로 Triplet Loss 네트워크를 훈련한다.
2. **최적화된 분류 모델 검증**: 딥러닝 특징 추출망을 통해 투영된 임베딩 특징 벡터들을 SVM(Support Vector Machine) 이진 분류 모델과 결합하여, 학습에 사용하지 않은 테스트 비디오에 대해 Real/Fake를 높은 신뢰도로 판별하는 최적의 가중치 모델(`triplet_effB4_ep06_BS28.pkl` 등)을 검증한다.
3. **이론의 실무 적용**: 학과 내 스터디 그룹을 통해 전공 수업인 「인공지능개론」에서 학습한 컴퓨터 비전 전처리 기법, 전이 학습(Transfer Learning), 메트릭 러닝 이론, 그리고 SVM 같은 전통적인 머신러닝 최적화 기법을 실제 AI 데이터 다운로드부터 분산 환경(Kaggle GPU)에서의 학습 및 전주기 파이프라인 구축에 적용하여 전문성을 강화한다.

### 2) 프로젝트 목표
 본 프로젝트는 딥페이크 영상의 미세한 왜곡 특징을 정밀하게 추출하고 오탐률을 최소화하여 실제 서비스나 대회 환경 등에서 안정적인 고성능을 낼 수 있는 강건한 모델 탐색을 목표로 한다. 구체적인 목표는 다음과 같다.
1. **고밀도 안면 전처리 파이프라인 정립**: MTCNN 기반의 실시간 안면 검출 및 정렬, 최적 마진(Margin) 부여 크롭 프로세스를 구축하여 영상 프레임 내 불필요한 배경 요소를 제거하고 연산 효율을 높인다.
2. **Triplet 임베딩 공간 확보**: 기준 얼굴(Anchor), 동일 인물의 얼굴(Positive), 딥페이크로 변조된 얼굴(Negative)의 임베딩 거리를 클래스 경계에 맞게 분할하는 Triplet Loss 기반 최적 가중치 모델을 구현한다.
3. **SVM 최종 판별기 성능 평가**: 딥러닝 피처 벡터의 비선형 분포를 선형 혹은 커널 함수로 효율적으로 나누는 SVM 분류기를 학습시켜, 정확도(Accuracy), F1-Score, ROC-AUC 지표 상에서 일반화 성능이 극대화된 탐지 구조를 도출한다.
4. **학습 협업 체계 모범 사례 제시**: 대용량 AI 데이터 수집(AIHub API) 및 분산 전처리, Kaggle 무료 GPU를 이용한 대규모 학습 체계 공유를 통해 멘토-멘티 간의 원활한 기술 협업 시스템의 모범 사례를 제시한다.

### 3) 멘토-멘티 협업
 본 프로젝트는 상명대학교 휴먼지능정보공학과 1학년 멘티와 2학년 멘토가 함께 진행한 전공 스터디 협업의 결과물이다. 2학년 멘토들은 이미지 전처리 라이브러리(OpenCV), TensorFlow/Keras 프레임워크 활용법, Triplet Loss 수식 유도 및 동작 원리, 최적화 하이퍼파라미터 튜닝 기법을 설명하고 리딩하였다. 1학년 멘티들은 AIHub 데이터 다운로드 스크립트 작성, MTCNN 전처리 파이프라인 검증, 검증 데이터에 대한 최종 추론 검증을 담당하며 주도적으로 참여했다.

---

## 2. 이론적 배경

### 1) 합성곱 신경망(CNN) 및 EfficientNet-B4
 이미지 데이터에서 공간적인 특징을 효과적으로 파악하고 필터 가중치를 공유하는 합성곱 신경망(CNN)은 컴퓨터 비전 분야의 핵심 기술이다. 그러나 모델의 깊이(Depth), 너비(Width), 해상도(Resolution)를 무작정 확장하는 것은 연산 복잡도와 모델 파라미터를 극도로 증가시켜 학습을 어렵게 만들고 오버피팅을 유발한다.

 본 연구에서 백본(Backbone)으로 선정한 **EfficientNet-B4**는 모델의 세 가지 확장 요소(깊이, 너비, 이미지 해상도)를 고정된 배율 상수로 동시에 고르게 스케일링하는 **Compound Scaling** 기법을 적용한 최적의 아키텍처이다. Mobile Inverted Bottleneck Convolution (MBConv) 구조를 기본 블록으로 채택하며, Squeeze-and-Excitation (SE) 최적화 모듈을 결합하여 매개변수 수 대비 고효율의 특징 추출 성능을 자랑한다. EfficientNet-B4는 입력 해상도로 $380 \times 380$ 크기를 표준으로 요구하며, 본 연구에서는 딥페이크 얼굴 프레임에서 미세한 픽셀 수준 변조 패턴을 고해상도로 캡처하기 위해 최적화된 백본으로 채택했다.

### 2) Triplet Loss (삼중항 손실 함수) 기반 임베딩 학습
 일반적인 분류 신경망은 Cross-Entropy Loss를 사용하여 클래스 레이블을 직접 매칭하지만, 이는 클래스 경계에 있는 애매한 데이터들에 대해 강건한 거리를 보장하지 못한다. 반면, **Metric Learning(도량형 학습)**은 데이터들을 고차원 임베딩 공간 상에 투영하여, 유사한 특징을 갖는 데이터 간의 거리는 좁히고 다른 특징을 갖는 데이터 간의 거리는 늘리도록 공간 구조 자체를 학습시킨다.

 본 연구에 적용된 **Triplet Loss(삼중항 손실 함수)**는 세 가지의 입력 샘플을 쌍으로 학습한다.
* Anchor ($A$): 기준 데이터 (예: 진짜 얼굴)
* Positive ($P$): Anchor와 동일한 클래스의 데이터 (예: 또 다른 진짜 얼굴)
* Negative ($N$): Anchor와 다른 클래스의 데이터 (예: 변조된 가짜 얼굴)

학습의 목표는 다음과 같은 거리 제약 조건식을 만족하는 것이다.

$$d(f(A), f(P)) + \alpha < d(f(A), f(N))$$

여기서 $f(\cdot)$는 네트워크가 생성하는 임베딩 벡터이며, $d(\cdot, \cdot)$는 두 벡터 간의 유클리드 거리 제곱이다. $\alpha$는 Anchor-Positive 거리와 Anchor-Negative 거리 사이에 확보하고자 하는 최소한의 안전 마진(Margin) 값이다. 

이를 손실 함수(Loss Function) 식으로 표현하면 다음과 같다.

$$\mathcal{L}_{triplet} = \sum_{i}^{N} \max \left( 0, \| f(A_i) - f(P_i) \|_2^2 - \| f(A_i) - f(N_i) \|_2^2 + \alpha \right)$$

 triplet_loss_dacon.py 에서는 이 중에서도 **Semi-hard Triplet Loss** 기법을 채택하여 학습의 수렴 속도를 높였다. Semi-hard 란 Anchor-Positive의 거리보다 멀리 있으면서도 마진 이내에 존재하여 학습 난도가 높은 적절한 Negative 샘플($d(A,P) < d(A,N) < d(A,P) + \alpha$)을 배치 내부에서 동적으로 선별(Masking 기법 활용)하여 가중치를 업데이트하는 진보된 메트릭 러닝 기법이다.

### 3) 서포트 벡터 머신 (SVM) 분류기
 서포트 벡터 머신(SVM)은 고차원의 공간에서 서로 다른 두 클래스(진짜 vs 가짜)를 가장 잘 분리해내는 최적의 초평면(Hyperplane)을 찾아내는 지도학습 알고리즘이다. 신경망의 최종 출력 레이어인 Softmax/Sigmoid 가 단순히 오차만을 기준으로 가중치를 선형 결합하는 것과 달리, SVM은 두 클래스 경계에 위치한 서포트 벡터(Support Vector)들 사이의 마진(Margin)을 최대화(Maximization)하도록 정식화되어 있어 수학적으로 강력한 강건성과 탁월한 일반화 성능을 보인다. 

 EfficientNet-B4 특징 추출기와 Triplet Loss 학습을 통해 획득한 512차원의 강건한 얼굴 임베딩 벡터를 SVM(Support Vector Classifier, SVC)의 입력으로 활용하여, 진짜 이미지와 가짜 이미지 간의 판별 성능을 최적화한 가중치 파일 `triplet_effB4_ep06_BS28.pkl`을 구성한다.

### 4) 성능 평가 지표
 딥페이크 탐지는 정적 오차가 아닌 정밀 분류가 중심이 되는 이진 분류 문제이다. 따라서 성능을 객관적으로 분석하기 위해 다음과 같은 혼동 행렬(Confusion Matrix) 기반의 지표를 정의한다.

1. **정확도(Accuracy)**: 전체 예측 개수 중에서 진짜와 가짜를 올바르게 분류해 낸 비율.
   $$\text{Accuracy} = \frac{TP + TN}{TP + TN + FP + FN}$$
2. **F1-Score**: 정밀도(Precision)와 재현율(Recall)의 조화 평균으로, 데이터 불균형 상황에서 모델의 조화로운 분류 성능을 검증하는 핵심 지표.
   $$\text{F1-Score} = 2 \times \frac{\text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}$$
3. **ROC-AUC (Area Under the Receiver Operating Characteristic Curve)**: 분류 임계값을 0에서 1까지 변화시킬 때, 모델이 진짜와 가짜를 올바르게 식별할 수 있는 전반적인 식별력과 변별력을 평면의 면적으로 표현한 것으로서 최고성능 1.0에 가까울수록 뛰어난 탐지 신뢰도를 나타낸다.

---

## 3. 데이터 수집 및 전처리

### 1) 데이터 수집 개요
 본 연구에서는 AIHub의 대규모 한국인 합성 영상(딥페이크) 데이터셋과 데이콘의 딥페이크 탐지 경진대회 데이터셋을 결합하여 실험을 구성하였다. 전체 데이터셋은 한국인 연령별 남녀의 원본 얼굴 비디오(Real)와 딥페이크 변조 도구(DeepFaceLab, Faceswap 등)를 사용하여 생성된 고화질 변조 비디오(Fake)로 구성되었다.

### 2) 얼굴 영역 크롭(Face Crop) 및 데이터셋 구성
 입력 영상들의 배경 영역 노이즈를 배제하고 얼굴의 내부 특징만을 안정적으로 추출하기 위해 다음과 같은 3단계 전처리 과정을 구축했다.

```
[입력] 원본 동영상 (.mp4) 
   │
   ├─ (Step 1) 비디오 프레임 추출 및 샘플링
   │     - 비디오 로딩 후 일정 간격(FPS)에 맞추어 JPG 이미지 프레임 추출
   │
   ├─ (Step 2) MTCNN 기반 고정밀 안면 인식 (Face Detection)
   │     - OpenCV Haar Cascade 대비 조명, 각도 변화에 강건한 MTCNN 모델 로드
   │     - 프레임에서 안면 영역의 Bounding Box 좌표 및 랜드마크 5포인트 검출
   │     - 만약 얼굴 인식이 실패한 경우 이미지를 180도 회전하여 재검출 시도
   │
   ├─ (Step 3) 마진(Margin) 적용 크롭 및 규격 표준화
   │     - 안면 경계선 외곽 정보를 포함하기 위해 가로 13.5%, 세로 10%의 여유 마진 부여
   │     - 크롭된 안면 영역 이미지를 EfficientNet-B4의 표준 규격인 380x380x3으로 리사이즈
   │     - 픽셀 값을 0.0 ~ 1.0 범위로 정규화
   │
   └─ [출력] 학습 및 검증용 크롭 안면 이미지 파일 (.jpg)
```

추출된 정제 데이터셋은 모델 학습용(Training Set) 80%, 일반화 평가 및 성능 검증용(Validation Set) 20%의 무작위 비율로 분할하여 성능 평가의 객관성을 보장하였다.

---

## 4. 실험 설계 및 구현 결과

### 1) 실험 설정 및 학습 환경
 본 연구의 딥러닝 특징 추출망 학습 및 SVM 모델 생성 최적화 환경의 주요 매개변수는 다음과 같다.

* **하드웨어 및 OS**: Linux Ubuntu OS / NVIDIA RTX 3090 GPU / Kaggle Cloud GPU 환경
* **개발 라이브러리**: Python 3.7+ / TensorFlow 2.x / Scikit-learn
* **학습 하이퍼파라미터**:
  * **Batch Size**: 28 (안정적인 메모리 관리와 Triplet Loss 배치 내 학습 최적화 고려)
  * **Epochs**: 6 (오버피팅 방지 및 Semi-hard Loss 수렴점 도달 확인)
  * **Input Shape**: (380, 380, 3) (EfficientNet-B4 정밀 해상도 적용)
  * **Embedding Dimension**: 512차원 특징 벡터 추출

### 2) EfficientNet-B4 + Triplet Loss + SVM 결합 모델 구현
 본 연구의 모델 학습 핵심 프로세스는 다음과 같이 2단계 파이프라인으로 구현되었다.

1. **Feature Extractor (특징 추출망) 학습**:
   - EfficientNet-B4 모델의 Classifier 부분을 제거하고, 512차원 Dense 레이어를 연결하여 L2 Normalization 레이어를 통과시킨 모델을 선언한다.
   - `triplet_loss_dacon.py`에서 구현된 Semi-hard Triplet Loss 함수를 목적 함수로 설정하여, 얼굴 이미지 배치 내에서 유사도가 높은 Positive 간의 거리는 가깝게, 난도가 높은 변조 이미지 Negative 간의 거리는 마진(margin=1.0) 이상으로 멀어지도록 오차 역전파를 수행한다.
   - 6 Epoch 완료 후 최적의 공간 매핑 성능을 가진 백본 가중치(`triplet_effB4_ep06_BS28.hdf5`)를 보존한다.
2. **SVM 이진 분류기 학습**:
   - 학습 완료된 특징 추출망에 전체 학습용 얼굴 이미지를 입력하여 고품질의 512차원 임베딩 피처 데이터를 확보한다.
   - 이 특징 벡터들과 라벨(0: Real, 1: Fake)을 Scikit-learn의 SVM (SVC, 커널=RBF, predict_proba 활성화) 모델에 입력하여 판별 경계를 계산한다.
   - 훈련된 SVM 모델 가중치를 `triplet_effB4_ep06_BS28.pkl` 파일로 직렬화하여 영구 저장한다.

### 3) 성능 비교 평가
 `triplet_effB4_ep06_BS28` 모델 구조의 우수성을 입증하기 위해, 단일 일반 CNN(Binary Cross-Entropy 기반 이진 분류기) 모델들과 탐지 정확도를 비교 평가한 결과는 다음과 같다.

| 모델 구조 | Backbone | 손실 함수 | 최종 분류기 | 검증 정확도 (Accuracy) | F1-Score | ROC-AUC |
| :--- | :--- | :--- | :--- | :---: | :---: | :---: |
| 단일 CNN (B0) | EfficientNet-B0 | Cross-Entropy | Dense (Sigmoid) | 88.42% | 0.875 | 0.921 |
| 단일 CNN (B4) | EfficientNet-B4 | Cross-Entropy | Dense (Sigmoid) | 91.15% | 0.906 | 0.948 |
| **제안 모델 (Triplet+SVM)** | **EfficientNet-B4** | **Semi-hard Triplet** | **SVM (RBF Kernel)** | **96.84%** | **0.965** | **0.989** |

 실험 결과, 단순 크로스 엔트로피 손실 함수와 선형 활성화 층을 사용한 모델들 대비, **EfficientNet-B4 백본 기반의 Triplet Loss 학습과 SVM 최종 분류기를 결합한 제안 모델**이 검증 정확도 **96.84%**, ROC-AUC **0.989**를 기록하여 오탐률을 크게 줄이고 가장 강건한 딥페이크 탐지 성공 성능을 보여주었다. 

---

## 5. 결론 및 제언

### 1) 결론
 본 연구에서는 정교한 안면 변조 딥페이크 영상을 강건하게 탐지하기 위하여 EfficientNet-B4 모델에 Semi-hard Triplet Loss 기반의 메트릭 러닝 기법과 SVM 머신러닝 최적 분류기를 융합한 최적화 모델 구조를 제안하였다. 
1. MTCNN 기반의 안면 바운딩 박스 검출 및 여유 마진 크롭 전처리는 배경의 노이즈를 배제하는 데 매우 기여했다.
2. Triplet Loss 학습을 통해 진짜 얼굴의 고유 미세 특징 분포는 밀집시키고 변조된 가짜 안면 특징은 원거리 공간으로 격리시킴으로써 차별화된 피처 임베딩 공간을 성공적으로 구축하였다.
3. RBF 커널 기반의 SVM 분류 모델은 일반화 분류 경계를 확실히 설정하여 단일 CNN 분류기 대비 탐지 정확도를 최대 **8.42%p** 가량 대폭 향상시키는 최적 성능을 달성하였다.

### 2) 한계점 및 제언
 비록 제안된 모델이 높은 정확도로 딥페이크 변조 영상을 분류해 냈으나 다음과 같은 실제 서비스 적용 한계점을 지닌다.
1. **환경적 요인 변화에 대한 취약성**: 극단적인 측면 얼굴 각도 변화, 매우 어둡거나 불균일한 조명 조건, 전송 및 손실 압축으로 인한 화질 저하(Low resolution) 환경에서는 MTCNN의 안면 검출 자체가 실패하거나 임베딩 특징 벡터의 왜곡으로 인해 오탐지가 유발되는 경향을 보였다.
2. **사전 정의된 템플릿의 정적 한계**: 프레임마다 개별 정적으로 이미지를 검출하여 분류하기 때문에 프레임 간의 연속적인 움직임이나 물리적 시계열 흐름을 학습하기 어렵다.

 **향후 고도화 방안**으로는, 본 연구에서 확인한 정적 임베딩 특징과 함께 **사람의 심장 박동에 따른 얼굴 피부의 미세한 혈류 색조 변화 패턴(PPG, 광용적맥파)**을 시계열 신호(예: CbCr 15배 증폭 및 DeepPhys LSTM 아키텍처) 형태로 추출해 내는 **멀티모달 생체 신호 하이브리드 탐지 기법**을 연구하여, 고도 압축 비디오나 정교한 딥페이크 공격에도 일반화된 세계 최고의 탐지 강건성을 달성하고자 한다.
